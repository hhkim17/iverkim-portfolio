# build output — regenerated by `node build-c.js` (GitHub Actions does this)
c/
iverkim-C-index.html

# local tooling config — holds machine-specific absolute paths
.claude/

# scratch / working files
works.js.bak
works.js.bak2
.DS_Store
**/.DS_Store
._*
